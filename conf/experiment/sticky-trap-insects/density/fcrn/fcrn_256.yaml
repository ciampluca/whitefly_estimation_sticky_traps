# @package _global_

defaults:
 - sticky-trap-insects/density/fcrn/common

data:
    train:
        patch_size: 256
    validation:
        patch_size: 256

optim:
    batch_size: 16
    batch_accumulation: 1