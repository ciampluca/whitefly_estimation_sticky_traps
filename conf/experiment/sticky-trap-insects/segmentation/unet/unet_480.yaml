# @package _global_

defaults:
 - sticky-trap-insects/segmentation/unet/common

data:
    train:
        patch_size: 480
    validation:
        patch_size: 480

optim:
    batch_size: 8
    batch_accumulation: 2