# @package _global_

defaults:
 - sticky-trap-insects/detection/fasterrcnn/common

data:
    train:
        patch_size: 480
    validation:
        patch_size: 480

optim:
    batch_size: 16
    batch_accumulation: 1
