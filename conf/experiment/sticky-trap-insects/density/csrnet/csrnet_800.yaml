# @package _global_

defaults:
 - sticky-trap-insects/density/csrnet/common

data:
    train:
        patch_size: 800
    validation:
        patch_size: 800

optim:
    batch_size: 8
    batch_accumulation: 2