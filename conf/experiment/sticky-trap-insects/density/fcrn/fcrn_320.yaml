# @package _global_

defaults:
 - sticky-trap-insects/density/fcrn/common

data:
    train:
        patch_size: 320
    validation:
        patch_size: 320

optim:
    batch_size: 12
    batch_accumulation: 1