# @package _global_

defaults:
 - sticky-trap-insects/density/csrnet/common

data:
    train:
        patch_size: 640
    validation:
        patch_size: 640

optim:
    batch_size: 12
    batch_accumulation: 1
