# @package _global_

defaults:
 - sticky-trap-insects/density/fcrn/common

data:
    train:
        patch_size: 800
    validation:
        patch_size: 800

optim:
    batch_size: 2
    batch_accumulation: 4